spring.application.name=miProyecto

# # DataSource settings
# spring.datasource.url=jdbc:mysql://localhost:3306/person?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # Hibernate settings
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.properties.hibernate.format_sql=true

# Chatbot integrations (configure endpoints / key as needed)
chatbot.n8n.url=http://localhost:5678/webhook/my-workflow

# Gemini (Google Generative Language) base URL and API key.
# Example usage: https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent?key=API_KEY
chatbot.gemini.url=https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent
chatbot.gemini.apiKey=
